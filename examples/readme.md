# Flexiwind Laravel Examples

This directory contains local Laravel projects used to test the Flexiwind Artisan commands while developing the CLI.

- `laravel-basic` is a minimal Laravel application wired to the local `unoforge/flexiwind-cli` and `unoforge/flexi-core` packages through Composer path repositories.
